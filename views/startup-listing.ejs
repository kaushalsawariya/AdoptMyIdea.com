<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AdoptMyIdea.com - List Your Startup</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }

        /* Hero Section */
        .hero-section {
            background: linear-gradient(135deg, rgba(30, 58, 138, 0.9), rgba(37, 99, 235, 0.9)), url('https://source.unsplash.com/1600x900/?startup,technology');
            background-size: cover;
            background-position: center;
            color: white;
            padding: 100px 20px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.4);
            z-index: 1;
        }

        .hero-section h1 {
            font-size: 3rem;
            font-weight: 700;
            line-height: 1.2;
            position: relative;
            z-index: 2;
            animation: fadeIn 1.5s ease-in-out;
        }

        .hero-section p {
            font-size: 1.25rem;
            margin-top: 1rem;
            line-height: 1.6;
            position: relative;
            z-index: 2;
            animation: fadeIn 2s ease-in-out;
        }

        .hero-section .btn-primary {
            background-color: white;
            color: #2563eb;
            padding: 12px 24px;
            border-radius: 8px;
            font-weight: 600;
            transition: background-color 0.3s ease, transform 0.3s ease;
            position: relative;
            z-index: 2;
            animation: fadeIn 2.5s ease-in-out;
        }

        .hero-section .btn-primary:hover {
            background-color: #f3f4f6;
            transform: scale(1.05);
        }

        /* Why List Section */
        .why-list-section {
            padding: 80px 20px;
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(10px);
            border-radius: 12px;
            margin: 40px auto;
            max-width: 1200px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
        }

        .why-list-card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 30px;
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            animation: slideUp 1.5s ease-in-out;
        }

        .why-list-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
        }

        .why-list-card h3 {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .why-list-card p {
            color: #6b7280;
            font-size: 1rem;
            line-height: 1.6;
        }

        /* Call to Action Section */
        .cta-section {
            background: linear-gradient(135deg, rgba(37, 99, 235, 0.9), rgba(30, 58, 138, 0.9));
            color: white;
            padding: 60px 20px;
            text-align: center;
            border-radius: 12px;
            margin: 40px auto;
            max-width: 1200px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
            animation: fadeIn 2s ease-in-out;
        }

        .cta-section h2 {
            font-size: 2.5rem;
            font-weight: 700;
        }

        .cta-section p {
            font-size: 1.25rem;
            margin: 1rem 0;
        }

        .cta-section .btn-secondary {
            background-color: white;
            color: #2563eb;
            padding: 12px 24px;
            border-radius: 8px;
            font-weight: 600;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .cta-section .btn-secondary:hover {
            background-color: #f3f4f6;
            transform: scale(1.05);
        }

        /* Footer */
        footer {
            background-color: #1f2937;
            color: #9ca3af;
            padding: 40px 20px;
            text-align: center;
        }

        footer a {
            color: #9ca3af;
            transition: color 0.3s ease;
        }

        footer a:hover {
            color: white;
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .line-clamp-3 {
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
    </style>
</head>
<body class="bg-gray-50 text-gray-900">
    <!-- Header -->
    <header class="bg-white shadow-sm">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
            <div class="text-xl font-bold text-blue-600">AdoptMyIdea.com</div>
            <!-- <nav class="space-x-4">
                <a href="#" class="text-gray-800 hover:text-blue-600">How It Works</a>
                <a href="#" class="text-gray-800 hover:text-blue-600">Pricing</a>
                <a href="#" class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">List Your Startup</a>
            </nav> -->
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container mx-auto">
            <h1>List Your Startup. <br> <span class="text-blue-300">Connect with the Right Buyer.</span></h1>
            <p>Join hundreds of founders using AdoptMyIdea.com to showcase their startups to serious investors, partners, and acquirers.</p>
            <div class="mt-6">
                <a href="/startup-sale-registration" class="btn-primary">List Your Startup Now</a>
            </div>
        </div>
    </section>

    <!-- Why List Section -->
    <section class="why-list-section">
        <div class="container mx-auto">
            <h2 class="text-2xl font-bold text-center mb-8">Why List on AdoptMyIdea.com?</h2>
            <div class="grid md:grid-cols-3 gap-6">
                <div class="why-list-card">
                    <h3>Investor Network</h3>
                    <p>Reach a network of verified investors and collaborators eager to support innovative startups.</p>
                </div>
                <div class="why-list-card">
                    <h3>Strategic Connections</h3>
                    <p>Get matched with partners, mentors, and service providers to accelerate your growth.</p>
                </div>
                <div class="why-list-card">
                    <h3>Full Control</h3>
                    <p>Maintain complete privacy and control over your startup listing and interactions.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Show 15 improved startup cards -->
    <section class="py-12 bg-gray-50">
        <div class="container mx-auto px-4">
            <h2 class="text-2xl font-bold mb-8 text-center text-blue-700">Featured Startups for Sale</h2>
            <div class="grid md:grid-cols-3 gap-8">
                <% if (startups && startups.length > 0) { %>
                    <% startups.forEach(function(startup) { %>
                        <div class="bg-white rounded-xl shadow-lg hover:shadow-2xl transition p-6 flex flex-col">
                            <div class="mb-4">
                                <h3 class="text-xl font-bold text-blue-800 mb-1"><%= startup.startupName %></h3>
                                <p class="text-gray-600 text-sm mb-2"><%= startup.location %> &middot; Founded <%= startup.incorporationYear %></p>
                                <p class="text-gray-700 mb-3 line-clamp-3"><%= startup.description %></p>
                            </div>
                            <div class="flex flex-wrap gap-2 text-xs text-gray-500 mb-4">
                                <span class="bg-blue-100 text-blue-700 px-2 py-1 rounded">Revenue: ₹<%= startup.monthlyRevenue %></span>
                                <span class="bg-green-100 text-green-700 px-2 py-1 rounded">Profit: ₹<%= startup.monthlyProfit %></span>
                                <span class="bg-yellow-100 text-yellow-700 px-2 py-1 rounded">Asking: ₹<%= startup.askingPrice %></span>
                            </div>
                            <div class="flex-1"></div>
                            <div class="mt-4 flex justify-between items-center">
                                <a href="<%= startup.website %>" target="_blank" class="text-blue-600 hover:underline font-semibold">Visit Website</a>
                                <span class="text-xs text-gray-400">By <%= startup.founderName %></span>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="col-span-3 text-center text-gray-500">No startups found.</div>
                <% } %>
            </div>
        </div>
    </section>

    <!-- Call to Action Section -->
    <section class="cta-section">
        <div class="container mx-auto px-4">
            <h2>Take Your Startup to the Next Level</h2>
            <p>Listing takes less than 5 minutes. No upfront costs. Start your journey to finding the right buyer or partner today.</p>
            <a href="/startup-sale-registration" class="btn-secondary">Get Discovered Now</a>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container mx-auto px-4 grid md:grid-cols-4 gap-8">
            <div>
                <h4>AdoptMyIdea.com</h4>
                <p>Connecting startups with their perfect match.</p>
            </div>
            <div>
                <h4>Quick Links</h4>
                <ul>
                    <li><a href="#">How It Works</a></li>
                    <li><a href="#">Pricing</a></li>
                    <li><a href="#">List Your Startup</a></li>
                </ul>
            </div>
            <div>
                <h4>Resources</h4>
                <ul>
                    <li><a href="#">Startup Guide</a></li>
                    <li><a href="#">Blog</a></li>
                    <li><a href="#">Support</a></li>
                </ul>
            </div>
            <div>
                <h4>Connect</h4>
                <div class="flex space-x-4">
                    <a href="#">Twitter</a>
                    <a href="#">LinkedIn</a>
                    <a href="#">Email</a>
                </div>
            </div>
        </div>
        <div class="container mx-auto px-4 mt-8 pt-8 border-t border-gray-800 text-center">
            <p>&copy; 2024 AdoptMyIdea.com. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>